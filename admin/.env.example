# Base URL of the Chowly API.
# Leave EMPTY in production: the API serves this build from the same origin, so
# the client falls back to the relative "/api/v1" and the auth cookie stays
# same-origin. Only set this when the admin is hosted separately from the API.
VITE_API_URL=
